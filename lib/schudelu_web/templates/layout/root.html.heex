<!DOCTYPE html>
<html lang="en" class="bg-black text-white">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="csrf-token" content={csrf_token_value()}>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,400;0,600;0,800;1,100;1,400;1,600;1,800&display=swap" rel="stylesheet">
    <%= live_title_tag assigns[:page_title] || "Schudelu", suffix: " · Phoenix Framework" %>
    <link phx-track-static rel="stylesheet" href={Routes.static_path(@conn, "/assets/app.css")}/>
    <script defer phx-track-static type="text/javascript" src={Routes.static_path(@conn, "/assets/app.js")}></script>
  </head>
  <body class="bg-black text-white p-4 pt-8 h-screen w-screen">
    <main>
    <%= @inner_content %>
    </main>
    <footer class="absolute bottom-0 left-0 right-0 h-12 w-screen px-4">
      <ol class="w-full h-full border-t-2 border-gray-dark flex justify-around pt-4">
        <li class="h-4 w-4"><%= link to: Routes.calendar_index_path(@conn, :index) do %><SchudeluWeb.SvgIconsView.icon name="home" /><% end %></li>
        <li class="h-4 w-4"><%= link to: Routes.calendar_index_path(@conn, :index) do %><SchudeluWeb.SvgIconsView.icon name="user" /><% end %></li><% #TODO redirect to the current active calendar %>
        <%= if function_exported?(Routes, :live_dashboard_path, 2) do %>
        <li class="h-4 w-4"><%= link to: Routes.live_dashboard_path(@conn, :home) do %><SchudeluWeb.SvgIconsView.icon name="layout" /><% end %></li><% #TODO redirect to the current active calendar %>
        <% end %>
      </ol>
    </footer>
  </body>
</html>
